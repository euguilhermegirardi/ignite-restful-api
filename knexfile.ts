// Import database's configuration to knex.
// I don't want to import the connection with the database, I just want to import the database's config.
import { databaseConfig } from './src/database'

export default databaseConfig
